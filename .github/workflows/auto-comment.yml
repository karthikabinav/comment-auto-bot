name: Automated Comment Bot

on:
  issues:
    types: [opened]

jobs:
  auto-comment:
    runs-on: ubuntu-latest
    permissions:
      issues: write
    steps:
      - name: Comment on bug issues
        if: contains(github.event.issue.labels.*.name, 'bug')
        run: echo "Thank you. We will fix it"
      - name: Comment on feature issues
        if: contains(github.event.issue.labels.*.name, 'feature')
        run: echo "Thank you, we will consider to include this feature."
      - name: Comment on unlabeled issues
        if: github.event.issue.labels.length == 0
        run: echo "Thank you for your contribution!"